name: Project

on:
  push:
    branches:
    - 'main'
    - 'refs/heads/release/**'
    paths:
    - 'project/src/service/**'
    - '.github/workflows/project.yml'
  workflow_dispatch:
  pull_request:
    types: [opened, ready_for_review, synchronize]
    branches:
    - 'main'
    - 'refs/heads/release/**'
    paths:
    - 'project/src/service/**'
    - '.github/workflows/project.yml'

jobs:
  build:
    if: github.event.pull_request.draft == false
    uses: norconsult-digital/platform/.github/workflows/dotnet-ci-jobs.yml@main
    with:
      dockerFile: 'project/src/service/Dockerfile'
      dockerBuildContext: './'
      repositoryName: 'isy360/project'
      migrationBundleArguments: '--project project/src/service/Infrastructure --startup-project project/src/service/Api'
      migrationBundleAppSettingsPath: 'project/src/service/Api'
      testProjects: 'project/src/service/Tests/**'
      nugetConfigPath: project/src/service
      dotnetVersion: '8.*'

  deploy:
    needs: build
    strategy:
      matrix:
        include:
        - environment: 'bowser-test'
          environmentName: 'test'
          branches: '["main"]'
          azureServicePrincipalId: ${{ vars.AZURE_BOWSER_NON_PROD_SERVICE_PRINCIPAL_ID }}
          dbConnection: ${{ vars.PROJECT_TEST_DB_CONNECTION }}
        - environment: 'bowser-stage'
          environmentName: 'stage'
          branches: '["release"]'
          azureServicePrincipalId: ${{ vars.AZURE_BOWSER_PROD_SERVICE_PRINCIPAL_ID }}
          dbConnection: ${{ vars.PROJECT_STAGE_DB_CONNECTION }}
        - environment: 'bowser-prod'
          environmentName: 'prod'
          branches: '["release"]'
          azureServicePrincipalId: ${{ vars.AZURE_BOWSER_PROD_SERVICE_PRINCIPAL_ID }}
          dbConnection: ${{ vars.PROJECT_PROD_DB_CONNECTION }}
    uses: norconsult-digital/platform/.github/workflows/dotnet-cd-jobs.yml@main
    with:
      environment: ${{ matrix.environment }}
      environmentName: ${{ matrix.environmentName }}
      allowedBranches: ${{ matrix.branches }}
      repositoryName: 'isy360/project'
      migrationBundleArguments: '--project project/src/service/Infrastructure --startup-project project/src/service/Api'
      azureServicePrincipalId: ${{ matrix.azureServicePrincipalId }}
      dbConnection: ${{ matrix.dbConnection }}
