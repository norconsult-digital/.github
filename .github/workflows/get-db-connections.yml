name: "Get DB Connections"

on:
  workflow_call:
    inputs:
      userApiUrl:
        description: "User Api Base Url"
        required: true
        type: string
      b2cDomain:
        description: "B2C Domain"
        required: true
        type: string
      b2cTenantId:
        description: "B2C Tenant ID to get access token"
        required: true
        type: string
      clientId:
        description: "ClientId ServiceUser/ApplicationId"
        required: true
        type: string
      connectionStringTemplate:
        description: "Connection string template with {DATABASE_NAME} placeholder (e.g., Server=sql.server.net,1433;Initial Catalog={DATABASE_NAME};Authentication=Active Directory Default;)"
        required: true
        type: string
    outputs:
      connectionStrings:
        description: "JSON array of database connection strings"
        value: ${{ jobs.fetch-db-instances.outputs.connectionStrings }}

jobs:
  fetch-db-instances:
    runs-on: ubuntu-latest
    outputs:
      connectionStrings: ${{ steps.parse_instances.outputs.connectionStrings }}
    
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in with Azure (Federated Creds)
        uses: azure/login@v2
        with:
          client-id: ${{ inputs.clientId }}
          tenant-id: ${{ inputs.b2cTenantId }}
          allow-no-subscriptions: true

      - name: Get Access Token
        id: get_token
        run: |
          ACCESS_TOKEN=$(az account get-access-token \
            --resource "https://${{ inputs.b2cDomain }}/${{ inputs.clientId }}" \
            --query accessToken -o tsv)
          echo "token=$ACCESS_TOKEN" >> $GITHUB_OUTPUT

      - name: Verify Access Token
        run: |
          if [ -n "${{ steps.get_token.outputs.token }}" ]; then
            echo "Token: ***"
          else
            echo "Token: Empty"
            exit 1
          fi

      - name: Get db names from user-api
        id: call_api
        env:
          ACCESS_TOKEN: ${{ steps.get_token.outputs.token }}
        run: |
          RESPONSE=$(curl -s -w "HTTPSTATUS:%{http_code}" -X GET \
            "https://${{ inputs.userApiUrl }}/api/databasesetup/instancenames" \
            -H "Authorization: Bearer ${ACCESS_TOKEN}")

          BODY=$(echo "$RESPONSE" | sed -e 's/HTTPSTATUS\:.*//g')
          STATUS=$(echo "$RESPONSE" | tr -d '\n' | sed -e 's/.*HTTPSTATUS://')

          echo "HTTP status: $STATUS"

          BODY_ESCAPED=$(echo "$BODY" | jq -c .)
          echo "response=$BODY_ESCAPED" >> $GITHUB_OUTPUT

      - name: Create connection strings
        id: parse_instances
        env:
          CONNECTION_TEMPLATE: ${{ inputs.connectionStringTemplate }}
        run: |
          # Parse database names from API response
          INSTANCES=$(echo '${{ steps.call_api.outputs.response }}' | jq -c '[.databaseInstances[].name]')
          echo "instances=$INSTANCES" >> $GITHUB_OUTPUT
          
          # Build connection strings from template
          CONNECTION_STRINGS=$(echo "$INSTANCES" | jq -c --arg template "$CONNECTION_TEMPLATE" '
            map(. as $name | $template | gsub("{DATABASE_NAME}"; $name))
          ')
          
          echo "connectionStrings=$CONNECTION_STRINGS" >> $GITHUB_OUTPUT