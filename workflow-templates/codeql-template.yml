name: "[general] CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '30 18 * * 3'

jobs:
  analyze-csharp:
    uses: norconsult-digital/.github/.github/workflows/codeql.yml
    with:
      language: csharp
      build-mode: none

  analyze-js:
    uses: norconsult-digital/.github/.github/workflows/codeql.yml
    with:
      language: javascript-typescript
      build-mode: none
